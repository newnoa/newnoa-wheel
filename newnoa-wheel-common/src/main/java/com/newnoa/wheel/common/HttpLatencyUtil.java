package com.newnoa.wheel.common;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import org.springframework.web.client.RestTemplate;

/**
 * newnoa-wheel.
 *
 * @author Neowei
 * @since 2024/4/10 20:06
 */
@Component
public class HttpLatencyUtil {
//    @Autowired
//    private RestTemplate restTemplate;
//
//    public long getQuarkusLatency(String host, int port) {
//        String response = restTemplate.getForObject(STR."http://\{host}:\{port}/actuator/health", String.class);
//        return 0L;
//    }
//
//
//    public long getSpringbootLatency(String host, int port) {
//        String response = restTemplate.getForObject(STR."http://\{host}:\{port}/actuator/health", String.class);
//        return 0L;
//    }
}
